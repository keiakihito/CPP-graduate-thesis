1. User access the system's front page. And it shows album link. When the user push this link the system navigate album page. It shows all the album in the database. Each link is clickable to go to audio track page.

　
2. User clicks the album title, then the all the tracks in the album is listed. All the tracks are also cllicable to go to the segmentation page.

3. User clicks the audio link, it goes to the audio wave form page. The system shows the track wave form. And it provides play, stop,  5sec ahead , 5 sec back button. User click play and audio plays and use can listen. On the audio region, the elastic box is provided which user can shrink or stretch. ALso the download button is also avaiale.

4. Use clicks the stop button, and music is stop.

5. While the user listen music, use hit the 5sec ahead or back button. Music moves or back but still keep playing the indicated point.

6. User can decide the starting point to get segmentation, and user adjust left side of box. When user hit play again, it play the left line of the box is the starting point.

7. When user decides the end of segmentaion, the user adjust right line of the box.

8. When user decides the region of the music, user push the download button. The backend cust the audion in the box field. Then user got download as audio track.

